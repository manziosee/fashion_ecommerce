<?xml version="1.0" encoding="UTF-8"?>
<odoo>
    <record id="view_fashion_sale_form" model="ir.ui.view">
        <field name="name">fashion.sale.form</field>
        <field name="model">sale.order</field>
        <field name="arch" type="xml">
            <form string="Sales Order">
                <sheet>
                    <group>
                        <!-- Make company_id invisible so domain works -->
                        <field name="company_id" invisible="1"/>
                        <field name="partner_id"/>
                        <field name="customer_type"/>
                    </group>
                    <notebook>
                        <page string="Order Lines">
                            <field name="order_line">
                                <tree editable="bottom">
                                    <field name="product_id"/>
                                    <field name="product_uom_qty"/>
                                    <field name="price_unit"/>
                                </tree>
                            </field>
                        </page>
                    </notebook>
                </sheet>
            </form>
        </field>
    </record>

    <record id="action_fashion_sales" model="ir.actions.act_window">
        <field name="name">Fashion Sales Orders</field>
        <field name="res_model">sale.order</field>
        <field name="view_mode">tree,form</field>
    </record>
</odoo>
